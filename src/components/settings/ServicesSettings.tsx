
import React from 'react';
import ServicesSettings from './services/ServicesSettings';

// Composant de redirection pour maintenir la compatibilité
const ServicesSettingsWrapper: React.FC = () => {
  return <ServicesSettings />;
};

export default ServicesSettingsWrapper;
